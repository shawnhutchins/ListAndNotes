doctype html

//link css
link(rel='stylesheet', href='/css/styleMain.css', type='text/css')

html
  head
    title NotesEdit
  body
    .noteContainer
      .noteEntry
        form(action="/notes/save" method="post")
          label Notes
          textarea(type="text" name="note" placeholder="Editing note")
            | #{note.note}
          .noteMenu
            span.noteMenuCell
              input(type="submit" value="Save")
